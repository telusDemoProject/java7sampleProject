CREATE TABLE USERS (
    id BIGINT NOT NULL GENERATED BY DEFAULT AS IDENTITY,
    name VARCHAR(50) NOT NULL,
    email VARCHAR(255) NOT NULL
);

-- Data will be inserted by data.sql

ALTER TABLE USERS ADD CONSTRAINT pk_users PRIMARY KEY (id); 